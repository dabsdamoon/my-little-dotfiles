Review the current git diff (staged and unstaged) for:

1. Correctness -- logic errors, off-by-one, null/undefined risks
2. Security -- injection, credential exposure, unsafe deserialization
3. Style -- consistency with existing codebase conventions
4. Simplicity -- unnecessary abstraction, dead code, over-engineering

Output: a numbered list of findings, sorted by severity (critical first). No preamble. If everything looks clean, say so in one sentence.
